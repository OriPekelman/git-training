# Version française — périmée

Ces fichiers sont l'ancienne version française du cours. Ils ne sont **pas** construits
par Hugo (seul `content/` l'est) et ils précèdent la révision de 2026.

L'anglais dans `content/` est désormais la source de référence. Cette version française
sera régénérée à partir de l'anglais ; ne pas y corriger quoi que ce soit en attendant,
ce serait du travail perdu.

Écarts connus par rapport à l'anglais actuel :

- références à Platform.sh, à supprimer (l'auteur n'y travaille plus) ;
- sorties de commandes datant de 2018-2022, plusieurs sont impossibles avec Git récent ;
- structure des parties différente : la nouvelle partie 4 (worktrees et agents, git notes,
  Git LFS, données et modèles, systèmes inspirés de Git) n'existe pas ici, et les anciennes
  parties 4 et 5 sont devenues 5 et 6 ;
- `master` / `main`, `git switch` / `git restore`, SHA-256 : rien de tout cela n'est traité.

---

# French version — out of date

These files are the old French version of the course. They are **not** built by Hugo
(only `content/` is) and they predate the 2026 revision. The English content under
`content/` is now the source of truth, and this translation will be regenerated from it.
Don't fix anything here in the meantime; it would be thrown away.
